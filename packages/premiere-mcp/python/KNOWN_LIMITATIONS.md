# 已知限制

- 当前只提供命令行模式，尚未提供 HTTP API。
- `detect_segments` 是轻量启发式分段，不是音乐结构识别模型。
- 节拍检测结果依赖音源质量与节奏清晰度，复杂现场录音会比电子鼓点更不稳定。
- 尚未实现从视频文件中显式抽音频的预处理封装；依赖 `librosa` 对输入媒体的读取能力。
- 尚未接入 `premiere-mcp` 的 Node 编排层，当前输出仍是独立 JSON 文件。
